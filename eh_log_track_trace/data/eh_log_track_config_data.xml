<?xml version="1.0" encoding="UTF-8" ?>
<odoo noupdate="1">
    <data>

        <!-- Public base URL placeholder. Operator overrides via the
             Settings page; the trackable mixin falls back to
             web.base.url if this row is removed. -->
        <record id="config_param_public_base_url" model="ir.config_parameter">
            <field name="key">eh_log_track_trace.public_base_url</field>
            <field name="value"></field>
        </record>

    </data>
</odoo>
